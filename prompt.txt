You are given text from a novel page. Convert it into transcript format.

Rules:
- Output ONLY a JSON array of objects.
- Each object must have exactly one key: either "Narrator" or the character's name.
- Do NOT include page numbers or any other wrapping keys.
- Preserve the order of sentences.
- Do not invent new content, only restructure the given text.

Text:
{page_text}
